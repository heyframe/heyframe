{%  block sw_channel_detail_content_tab_theme %}
    <sw-tabs-item :disabled="isProductComparison || isLoading"
                  :route="{ name: 'sw.sales.channel.detail.theme', params: { id: $route.params.id } }"
                  :title="$tc('sw-channel.detail.tabTheme')">
        {{ $tc('sw-channel.detail.tabTheme') }}
    </sw-tabs-item>
{% endblock %}
