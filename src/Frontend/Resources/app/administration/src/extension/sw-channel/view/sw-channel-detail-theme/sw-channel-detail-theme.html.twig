{% block sw_channel_detail_theme %}
    <sw-card :title="$tc('channel-theme.title')" :isLoading="isLoading">
        <div class="sw-channel-detail-theme">

            {% block sw_channel_detail_theme_preview %}
                <div class="sw-channel-detail-theme__preview">
                    {% block sw_channel_detail_theme_preview_item %}
                        <div @click="openThemeModal">
                            <sw-theme-list-item
                                :theme="theme"
                                :disabled="!acl.can('channel.editor')"
                                :active="!!theme">
                            </sw-theme-list-item>
                        </div>
                    {% endblock %}

                    {% block sw_channel_detail_theme_modal %}
                        <sw-theme-modal
                            v-if="showThemeSelectionModal"
                            :selected-theme-id="theme?.id"
                            @modal-theme-select="onChangeTheme"
                            @modal-close="closeThemeModal">
                        </sw-theme-modal>
                    {% endblock %}
                </div>
            {% endblock %}

            {% block sw_channel_detail_theme_info %}
                <div class="sw-channel-detail-theme__info">

                    {% block sw_channel_detail_theme_info_content %}
                        <div class="sw-channel-detail-theme__info-content">

                            {% block sw_channel_detail_theme_info_name %}
                                <div class="sw-channel-detail-theme__info-name" :class="{ 'is--empty': !theme }">
                                    {{ theme ? theme.name : $tc('channel-theme.defaultTitle') }}
                                </div>
                            {% endblock %}

                            {% block sw_channel_detail_theme_info_author %}
                                <div v-if="theme" class="sw-channel-detail-theme__info-author">
                                    {{ theme.author }}
                                </div>
                            {% endblock %}

                            {% block sw_channel_detail_theme_info_description %}
                                <div v-if="theme && theme.description" class="sw-channel-detail-theme__info-description">
                                    <p class="sw-channel-detail-theme__info-description-title">
                                        {{ $tc('sw-theme-manager.detail.description') }}:
                                    </p>
                                    <p>{{ theme.description |truncate(140) }}</p>
                                </div>
                            {% endblock %}
                        </div>
                    {% endblock %}

                    {% block sw_channel_detail_theme_info_actions %}
                        <div class="sw-channel-detail-theme__info-actions">

                            {% block sw_channel_detail_theme_info_actions_theme %}
                                <sw-button size="small" @click="openThemeModal" :disabled="!acl.can('channel.editor')">
                                    {{ theme ? $tc('channel-theme.changeTheme') : $tc('channel-theme.changeThemeEmpty') }}
                                </sw-button>
                            {% endblock %}

                            {% block sw_channel_detail_theme_info_actions_manager %}
                                <sw-button size="small" @click="openInThemeManager" :disabled="!acl.can('channel.editor')">
                                    {{ theme ? $tc('channel-theme.editContent'): $tc('channel-theme.createTheme') }}
                                </sw-button>
                            {% endblock %}
                        </div>
                    {% endblock %}
                </div>
            {% endblock %}
        </div>
    </sw-card>
{% endblock %}
