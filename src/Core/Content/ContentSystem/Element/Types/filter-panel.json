{
  "type": "Sw:Filter:Panel",
  "category": "service",
  "label": "Filter Panel",
  "description": "Displays filter options based on product aggregations",
  "properties_schema": {
    "type": "object",
    "properties": {}
  },
  "metadata": {
    "note": "Filter panel uses aggregations from listing context to build filter options"
  }
}
